**Database** : A structured collection of data, typically stored electronically, organized to allow efficient storage, retrieval, and management. 
It’s the actual data and its structure (e.g., tables, rows, columns in a relational database).
